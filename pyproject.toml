[project]
name = "wcsi"
version = "1.0.0"
dependencies = [
  "cartopy",
  "huracanpy",
  "matplotlib",
  "numpy",
  "pandas",
  "scipy",
  "tqdm",
  "docopt",
]

[project.scripts]
wcsi = "wcsi.scripts.wcsi:main"
